export const STORAGE_KEYS = {
  documentWhitelist: 'lexchain:document-whitelist',
  documentsStore: 'lexchain:documents-store',
  authToken: 'lexchain.auth-token',
  refreshToken: 'lexchain.refresh-token',
} as const;
